# EToH XL Auto Publisher
## using xl auto publisher:
* download this repository by clicking "download zip" through the green code button
* unzip and open this folder then right click inside it and select "open in terminal"
run this command:
```
$env:ROBLOSECURITY = "blank"
$env:ROBLOX_API_KEY = "blank"
.\lune run publish.luau
```
